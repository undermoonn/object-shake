{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "cache": false,
      "outputs": ["dist/**", "es/**", "types/**"]
    },
    "publish": {
      "dependsOn": ["^build"],
      "cache": false,
      "outputs": []
    }
  }
}
